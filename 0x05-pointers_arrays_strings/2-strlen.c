#include "main.h"
#include <stdio.h>
#include <string.h>

/*
 * _strlen - returns the length of a string
 * @s : the string to get the length of
 * return : length of a string
 */

int _strlen(char *s)

{
	int len;
	len = _strlen(char *s);
        
	_putchar( "%d/n", len);
	return (len);
}
