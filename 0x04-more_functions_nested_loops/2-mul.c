#include "main.h"

/**
 * mul - multiplies two integers
 * @a : first number to multiply with
 * @b : second number to multipy with first number
 * Return: result of multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}


