#include <stdio.h>
/**
  *main-entry point
  *Description : 'displays lowercase alphabets from a to z except q and e'
  * Return: always 0
  */
int main(void)
{
        int n = 97;

        while (n < 123)
	for (n; is not 101; n; is not 113)
        {
                putchar(n);
                n++;
        }
        putchar('\n');
        return (0);
}
